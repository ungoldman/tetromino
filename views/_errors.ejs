<% if(typeof(messages) != 'undefined' && messages != '') { %>
  <% String(messages).split(',').forEach(function(message) { %>
    <div class="alert alert-error">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <strong>Woops!</strong>
      <%= message %>
    </div>
  <% }) %>
<% } %>
